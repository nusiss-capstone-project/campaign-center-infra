# ServiceAccount used by External Secrets Operator to authenticate to Vault (Kubernetes auth).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ external_secrets_vault_sa_name }}
  namespace: {{ external_secrets_namespace }}
  labels:
    app.kubernetes.io/name: external-secrets
    app.kubernetes.io/component: vault-auth
